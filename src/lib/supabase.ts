import { createClient } from '@supabase/supabase-js';

    return import.meta.env[key] || '';
  return '';
    return import.meta.env[key] || '';
  }
  return '';
};

export const supabase = hasConfig
      auth: {

      },

export const getSupabaseConfig = 
    url: supabaseUrl,
  };

  return {
    url: supabaseUrl || 'Not conf
  };









